# from datetime import datetime
# import os
# from influxdb_client import InfluxDBClient, Point, WritePrecision
# from influxdb_client.client.write_api import SYNCHRONOUS
# influxdb_token='zkni4pQaUq8yDKkqRZqRgjeI9DKqYPLRWXwBcYYvtSSNqItjVPGISIF12NTLx5fsajKCIWQxTG-0Tl5V38N4ng=='
# # os.environ.get('INFLUXDB_TOKEN')
# org = "admin"
# bucket = "admin"

# with InfluxDBClient(url="http://localhost:8086", token=influxdb_token, org=org) as client:
#     client.create_database('Crypto')
#     # write_api = client.write_api(write_options=SYNCHRONOUS)
#     # point = Point("mem").tag("host", "host1").field("used_percent", 23.43234543).time(datetime.utcnow(), WritePrecision.NS)

#     # write_api.write(bucket, org, point)
#     # query = 'from(bucket: "admin") |> range(start: -1h)'
#     # tables = client.query_api().query(query, org=org)
#     # for table in tables:
#     #     for record in table.records:
#     #         print(record)
#     # client.close()
